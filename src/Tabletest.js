import React, { useState } from "react";

function Tabletest(setter, settters) {
  const [clicker, setclicker] = useState("");
  return (
    <div>
      <h1>testing</h1>
    </div>
  );
}

export default Tabletest;
